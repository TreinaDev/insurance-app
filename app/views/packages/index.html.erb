<h1 class="my-3"><%= t '.title' %></h1>

<% if @packages.any? %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= PendingPackage.human_attribute_name(:product_category) %></th>
        <th><%= PendingPackage.human_attribute_name(:insurance_company) %></th>
        <th><%= PendingPackage.human_attribute_name(:name) %></th>
        <th><%= PendingPackage.human_attribute_name(:min_period) %></th>
        <th><%= PendingPackage.human_attribute_name(:max_period) %></th>
        <th><%= Package.human_attribute_name(:price) %> Mensal</th>
      </tr>
    </thead>
    <tbody>
      <% @packages.each do |package| %>
            <tr>
              <td><%= package.pending_package.product_category.name %></td> 
              <td><%= link_to package.pending_package.insurance_company.name, package.pending_package.insurance_company, class: "link-dark"  %></td>
              <td><%= package.pending_package.name %></td>
              <td><%= package.pending_package.min_period %> meses</td>
              <td><%= package.pending_package.max_period %> meses</td>
              <td><%= number_to_percentage(package.price, precision: 2) %></td>
            </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="alert alert-secondary text-center">
    <%= t '.message' %>
  </div>
<% end %>
